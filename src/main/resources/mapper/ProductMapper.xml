<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.mapper.ProductMapper">
    <insert id="insert">
        insert into product (id, goods_id, goods_specification_ids, goods_sn, goods_number, retail_price, cost, goods_weight, has_change, goods_name, is_on_sale, is_delete)
        values ();
    </insert>
</mapper>